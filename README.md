# 文生视频流水线

Agent 自动化方案与可运行实现见 [`video_pipeline/README.md`](video_pipeline/README.md)。

```bash
python3 -m pip install -r video_pipeline/requirements.txt
PYTHONPATH=. python3 -m video_pipeline --theme "你的主题" --provider mock --tts mock
```
